name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  lint-test:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: lint-test (ubuntu-latest)
            os: ubuntu-latest
          - name: lint-test (macos-latest)
            os: macos-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - uses: astral-sh/setup-uv@v7
        with:
          version: "0.11.7"
          enable-cache: false
      - uses: actions/cache@v4
        with:
          path: ~/.cache/uv
          key: uv-${{ runner.os }}-py3.12-${{ hashFiles('uv.lock') }}
      - name: Sync dependencies
        run: make sync
      - name: Smoke CLI help
        run: |
          uv run webinar-transcriber --help
      - name: Smoke whisper runtime
        run: |
          uv run python -c "from pywhispercpp.model import Model; assert Model.system_info().strip()"
      - name: Run tests
        run: make check
      - name: Install LLM extra
        run: uv sync --extra llm --group dev
      - name: Run LLM tests
        run: uv run pytest --no-cov webinar_transcriber/tests/test_llm.py
