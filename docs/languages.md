# Language Support

The initial release targets automatic language detection via `faster-whisper`.

## Supported Focus Languages

- English
- Russian

These languages are first-class in fixtures, examples, and OCR fallback mapping. The internal
architecture will remain generic so additional languages can be added later without changing the
CLI.
